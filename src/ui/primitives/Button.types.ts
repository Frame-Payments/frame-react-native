/**
 * Visual treatment applied to a {@link Button}.
 * - `'primary'` — filled background using `theme.colors.primaryButton`.
 * - `'secondary'` — transparent background with `theme.colors.secondaryButtonText`.
 */
export type ButtonVariant = 'primary' | 'secondary';
